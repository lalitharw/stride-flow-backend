services:
  redis:
    container_name: stride_flow_backend_redis
    image: redis:trixie
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes

volumes:
  redis_data: